import styled from 'styled-components';

const PreviousEquation = styled.div`
  font-size: 20px;
  overflow-x: auto;
  padding: 24px 24px 0 24px;
  text-align: right;
  white-space: nowrap;
`;

export { PreviousEquation };
