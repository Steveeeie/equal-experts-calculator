import styled from 'styled-components';

const CurrentEquation = styled.div`
  font-size: 40px;
  line-height: 72px;
  overflow-x: auto;
  padding: 0 24px;
  text-align: right;
  white-space: nowrap;
`;

export { CurrentEquation };
